import clsx from "clsx";
import { TextInput, View } from "react-native";

const PinInput = () => {
  return (
    <View></View>
    // <View className="flex-row items-center w-full px-4 focus:border-secondary rounded-tr-xl rounded-bl-xl rounded-br-app rounded-tl-app">
    //   <TextInput
    //     className={clsx("flex-1 font-input text-foreground")}
    //     value={"0"}
    //     // value={value.toString()}
    //     // placeholder={placeholder}
    //     // placeholderTextColor={grayColorPlaceholder}
    //     // onChangeText={onChangeText}
    //     // secureTextEntry={showPassword}
    //     // // textContentType={showPassword ? "none" : "password"}
    //     // textContentType={"oneTimeCode"}
    //     // onBlur={onBlur}
    //     // editable={!disabled}
    //     // keyboardType={keyboardType}
    //     maxLength={1}
    //   />
    //   {/* {isPassword ? (
    //           <TouchableOpacity onPress={() => setShowPassword((prev) => !prev)}>
    //             <Image
    //               source={!showPassword ? images.eye_closed_dark : images.eye_dark}
    //               className="w-8 h-8"
    //               resizeMode="contain"
    //             />
    //           </TouchableOpacity>
    //         ) : null} */}
    // </View>
  );
};
export default PinInput;
